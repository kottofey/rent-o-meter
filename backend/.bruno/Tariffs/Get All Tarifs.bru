meta {
  name: Get All Tarifs
  type: http
  seq: 2
}

get {
  url: {{BASE_URL}}/tarifs?scopes[tarif:byType]=water_in
  body: none
  auth: inherit
}

params:query {
  "scopes[tarif:byType]": water_in
}

settings {
  encodeUrl: true
  timeout: 0
}
