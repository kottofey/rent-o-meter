meta {
  name: Get All Agreements
  type: http
  seq: 3
}

get {
  url: {{BASE_URL}}/agreements
  body: none
  auth: inherit
}

params:query {
  ~scopes[]: isExpiredAndActive
  ~scopes[]: isActual
  ~scopes[]: isActive
  ~scopes[]: isExpired
  ~includes[]: Rentee
}

settings {
  encodeUrl: true
  timeout: 0
}
